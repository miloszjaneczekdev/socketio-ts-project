# Guess The Code Client

React/Vite frontend for the Guess The Code Socket.IO game.

## Scripts

```bash
npm run dev
npm run build
npm run lint
npm run preview
```

The client expects the Socket.IO server at `http://localhost:3000/`.
